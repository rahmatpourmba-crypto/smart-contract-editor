# 📘 راهنمای استقرار توکن (برای مشتری)

این راهنما را بعد از تحویل فایل `.sol` به مشتری بفرست. شامل: استقرار، افزودن توکن به کیف پول و تست اول.

---

## روش ۱ — استقرار با Remix (پیشنهادی)

1. به `https://remix.ethereum.org` برو.
2. در پنل فایلها، فایل `GLDToken.sol` را بساز (اسم دلخواه است) و کل کدی که گرفتی را در آن بچسبان.
3. تب **Solidity Compiler** را باز کن:
   - نسخه را روی **0.8.20 یا بالاتر** بگذار.
   - دکمه **Compile** را بزن. باید ببینی «Compilation successful».
4. تب **Deploy & Run Transactions** را باز کن:
   - در «Environment» گزینه **Injected Provider - MetaMask** را انتخاب کن.
   - کیف پولت را انتخاب کن (صفحه MetaMask باز میشود → Connect).
   - دکمه **Deploy** را بزن.
   - در MetaMask تراکنش را **تأیید** کن (گس از حساب تو کسر میشود).
5. چند ثانیه بعد، قرارداد مستقر میشود. آدرس قرارداد را از پایین کپی کن.

---

## روش ۲ — استقرار با ادیتور آنلاین

1. به `https://rahmatpourmba-crypto.github.io/smart-contract-editor/` برو.
2. کد را در ادیتور بگذار و **Compile** را بزن (همینجا کامپایلشده هم تحویل گرفته شد).
3. تب **Bytecode** را باز کن و کل بایتکد را کپی کن.
4. به Remix برو → تب Deploy → Environment را **Injected Provider** بگذار و بهجای Deploy از بخش «At Address»/bytecode استفاده کن — یا سادهتر: همان روش ۱ را با همین فایل انجام بده (کد یکسان است).

> سادهترین راه همان روش ۱ است؛ این دو ادیتور برای ساختن/تست کد هستند.

---

## افزودن توکن به کیف پول

### متامسک (MetaMask)
1. MetaMask را باز کن → روی دکمه **Import tokens** (یا + Add Token) بزن.
2. آدرس قرارداد توکن را کپی کن → بچسبان.
3. نماد و اعشار را خودش پیدا میکند (GLD / 18).
4. **Add** را بزن. توکن در کیف پولت ظاهر میشود.

### تراست ولت (Trust Wallet)
1. تراست ولت را باز کن → روی نماد کیف پول در بالا → **Add Custom Token** (یا گزینه Receive و سپس Custom Token).
2. شبکه را انتخاب کن (مثلاً BNB Smart Chain یا Polygon — هماهنگ با شبکهای که توکن روی آن مستقر شد).
3. آدرس قرارداد را بچسبان → نماد و اعشار را خودش پر میکند → **Done**.
4. توکن اضافه شد.

### افزودن شبکه به کیف پول (اگه شبکه هنوز نیست)
- **BNB Smart Chain:** Chain ID `56` — RPC: `https://bsc-dataseed.binance.org` — توکن: BNB
- **Polygon:** Chain ID `137` — RPC: `https://polygon-rpc.com` — توکن: POL (یا MATIC)
- **Ethereum:** خودکار در MetaMask هست.

---

## تست اول (بعد از استقرار)

1. آدرس توکن را در مرورگر شبکه ببین (BscScan / Polygonscan / Etherscan) — باید نام، نماد و عرضهات را نشان بدهد.
2. در کیف پولت، موجودی = کل عرضه اولیه (مالک هستی).
3. یک **انتقال آزمایشی** بین دو کیف خودت انجام بده (مثلاً ۱۰۰ توکن).
4. اگر نسخه **پریمیوم** است:
   - انتقال از کیف **مالک** → بدون مالیات (کل ۱۰۰ میرسد).
   - انتقال از کیف **عادی** → مالیات طبق تنظیمات کسر میشود (مثلاً ۵٪)؛ بخشی سوزانده و بخشی به کیف درآمد میرود.
5. اگر خطایی دیدی، اسکرینشات بفرست تا بررسی کنم.

---

## نکات مهم امنیتی

- هرگز عبارت بازیابی (Seed Phrase) کیف پولت را برای کسی نفرست — حتی برای من.
- آدرس قرارداد را قبل از هر کاری فقط از کیف پول خودت یا مرورگر شبکه تأیید کن.
- گس را با مقدار پیشنهادی کیف پول بپرداز؛ عجله نکن.
- فایل کد را دستکاری نکن مگر اینکه بدانی چه میکنی — هر تغییری ممکن است کد را خراب کند.

---

## چرا پرداخت ۵۰٪ بعد از استقرار؟

طبق قرارداد: ۵۰٪ پیشپرداخت برای تحویل کد، و ۵۰٪ بعد از اینکه توکن روی شبکه آنلاین شد. اینطوری هر دو طرف مطمئن میشوند.
