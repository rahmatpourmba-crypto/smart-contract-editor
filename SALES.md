# راهنمای فروش قرارداد MyToken (ERC-20)

این سند برای فروش نمونه‌کار توکن به مشتریان واقعی است. همه‌چیز را گام‌به‌گام توضیح می‌دهد: قیمت‌گذاری، نحوه پیدا کردن مشتری، تحویل و پشتیبانی.

---

## ۱. چه چیزی می‌فروشی؟

یک **قرارداد توکن استاندارد ERC-20 آماده استقرار** با این امکانات:

| ویژگی | توضیح |
|-------|-------|
| ERC-20 استاندارد | Transfer, Approve, TransferFrom, Allowance |
| Mint | مالک می‌تواند توکن جدید ضرب کند |
| Burn | سوزاندن توکن (کاهش عرضه) |
| Lock | قفل کردن توکن‌های یک حساب تا زمان مشخص |
| forceUnlock | آزادسازی زودهنگام توسط مالک |
| transferOwnership | انتقال مالکیت |
| بدون وابستگی خارجی | هیچ import خارجی نیست — امن و قابل ممیزی |
| ۱۶ تست خودکار | همه عبور کرده‌اند (ganache + ethers) |

مشتری می‌گیرد:
- کد کامل `MyToken.sol` (با مجوز استفاده)
- راهنمای استقرار
- تنظیم نام توکن، نماد و عرضه اولیه
- پشتیبانی فنی تا زمان استقرار موفق

### نسخه پریمیوم (`PremiumToken.sol`)
همه‌چیز نسخه پایه + قابلیت‌های درآمدزا و حرفه‌ای:

| ویژگی | توضیح |
|-------|-------|
| مالیات هر انتقال | درصد دلخواه از هر تراکنش (مثلاً ۵٪) |
| سوزاندن خودکار | بخشی از مالیات سوزانده می‌شود (افزایش ارزش) |
| دریافت درآمد (Marketing) | باقی مالیات به کیف پول درآمد می‌رود |
| ضد نهنگ (Anti-Whale) | سقف نگهداری هر کیف (مثلاً ۲٪ عرضه) |
| سقف هر تراکنش (MaxTx) | سقف حجم هر انتقال (مثلاً ۲۰٪ عرضه) |
| مالیات خرید/فروش جداگانه | درصد مالیات Buy و Sell می‌تواند متفاوت باشد |
| وایت‌لیست | فقط آدرس‌های مجاز می‌توانند بخرند/انتقال بدهند |
| توقف اضطراری (Pausable) | مالک می‌تواند همه انتقال‌ها را موقتاً متوقف کند |
| بلاک‌لیست | مسدود کردن آدرس‌های خاص (مثلاً کلاهبرداران) |
| معافیت مالیاتی | مالک، کیف‌های مجاز و صرافی از مالیات و سقف معاف |
| سوییچ مالیات | مالک می‌تواند مالیات را روشن/خاموش کند |

این نسخه مخصوص پروژه‌هایی است که به‌دنبال درآمد پایدار (لیکوییدیتی خودکار، تیم، مارکتینگ) هستند.

### مدل فروش: فقط کد (بدون دیپلوی)

فروش ما **فقط تحویل کد `.sol`** است — بدون دیپلوی، بدون اتصال کیف پول، بدون گس. گس و کیف پول فقط موقع استقرار توسط خود مشتری در Remix لازم است (اختیاری: با پشتیبانی استقرار +۵۰ دلار تو به او در استقرار کمک می‌کنی).

---

## ۱.۵. رقبا — فکتوری‌های بدون کدنویسی (مثل PolyMint)

سرویس‌هایی مثل PolyMint با ۸ تا ۱۲ دلار توکن ERC-20 می‌فروشند. تو نباید با قیمت بجنگی؛ باید با **مالکیت و امنیت** بجنگی:

| | فکتوری ارزان (۸–۱۲ دلار) | بسته تو (۵۰ دلار+) |
|---|---|---|
| کد قرارداد مال مشتری است | ❌ فقط از فکتوری مشترک | ✅ فایل `.sol` را می‌گیرد |
| ممیزی کد | ❌ قابل بررسی نیست | ✅ کد ساده و قابل بررسی |
| مالیات/سوزاندن/ضد نهنگ | ❌ فقط توکن خالی | ✅ نسخه پریمیوم |
| شبکه‌ها | فقط یک شبکه (معمولاً Polygon) | BNB / Ethereum / Polygon / Tron |
| ریسک دسترسی ادمین فکتوری | ⚠️ ادمین فکتوری می‌تواند mint/فریز کند | ✅ مالکیت کامل فقط با مشتری |
| پشتیبانی | ❌ هیچ تماسی وجود ندارد | ✅ تا آنلاین شدن + ۳۰ دقیقه تست |
| دامنه/اعتماد | ⚠️ معمولاً دامنه رایگان (workers.dev و...) | ✅ مخزن عمومی + نمونه‌کار |

**اسکریپت جواب وقتی مشتری به سرویس ارزان اشاره می‌کند:**

> اون ۸ دلار فقط یه توکن خالی روی Polygon است. قراردادش توی دسترس تو نیست و چون از یه فکتوری مشترک میاد، ادمینش می‌تونه توش دست ببره (mint یا فریز). تو با بسته ما کد کامل را مال خودت می‌گیری، روی هر شبکه‌ای که بخوای deploy می‌کنی، و اگه بخوای مالیات و ضد نهنگ هم می‌ذاریم. فرق «ساخت سریع» با «مالکیت واقعی» است.

**نکته:** سرویس ارزان قیمت را برای مشتریِ بودجه‌ای به‌عنوان «دمو» پیشنهاد نکن؛ همین مقایسه را نشان بده و بگو قیمت ما شامل کد، تست، شبکه‌های متعدد و پشتیبانی واقعی است.

---

## ۲. قیمت‌گذاری

| بسته | محتوا | قیمت پیشنهادی |
|------|-------|---------------|
| پایه | کد قرارداد + راهنمای استقرار + پشتیبانی چت | ۳۰ تا ۵۰ دلار |
| پریمیوم | مالیات، سوزاندن خودکار، ضد نهنگ، سقف تراکنش + راهنما | ۵۰ تا ۱۰۰ دلار |
| سفارشی | هر ترکیبی از قابلیت‌های پریمیوم (وایت‌لیست، مالیات خرید/فروش جدا، Pausable، Blacklist و...) | ۱۰۰ تا ۳۰۰ دلار |
| پشتیبانی استقرار | راهنمایی/کمک برای دیپلوی مشتری در Remix | +۵۰ دلار |

> قیمت را بر اساس بازار و کشور مشتری تنظیم کن. برای شروع، از قیمت پایین شروع کن تا نمونه‌کار و امتیاز بسازی، بعد قیمت را بالا ببر.

---

## ۳. از کجا مشتری پیدا کنم؟

1. **تلگرام** — گروه‌های ارز دیجیتال و توکن‌سازی (مثلاً گروه‌های فارسی و انگلیسی مربوط به NFT و توکن)
2. **توییتر/X** — جستجوی کلمات "توکن بساز"، "create token", "mint token"
3. **فریلنسر مارکت‌ها** — پونیشا، پارس‌فریلنسر، Upwork، Fiverr (پروفایل بساز)
4. **گروه‌های دیسکورد** پروژه‌های کریپتو
5. **کدینگ سرویس‌ها** — ارائه پکیج آماده به کسانی که پروژه راه می‌اندازند
6. **پیشنهاد به صاحبان استارتاپ‌ها** که می‌خواهند توکن اختصاصی برای جامعه‌شان داشته باشند

---

## ۴. مکالمه اول با مشتری (اسکریپت)

> سلام، یه قرارداد توکن استاندارد ERC-20 آماده دارم که با Solidity 0.8 نوشته شده، بدون وابستگی خارجی، تست‌شده و امن. اسم، نماد و عرضه اولیه توکن شما رو می‌گیرم و تحویل می‌دم. اگر بخواید خودم هم استقرارش می‌کنم. هزینه چقدره؟...

این پیام را شخصی‌سازی کن و **عجله نکن**. اول نیاز مشتری را بشنو.

---

## ۵. قبل از فروش چک کن

- [ ] نام و نماد توکن مشتری (مثلاً "Gold Token" / "GLD")
- [ ] عرضه اولیه (چند میلیون؟)
- [ ] مالک قرارداد کیست (آدرس کیف پول مشتری)
- [ ] آیا قابلیت اضافه لازم است (Mint پذیری، مالیات، وایت‌لیست، Pausable، Blacklist، سقف تراکنش)؟
- [ ] کدام شبکه (Ethereum، BNB Smart Chain، Polygon، Tron، دیگر)
- [ ] بودجه و روش پرداخت (کریپتو / پرفکت مانی / ریال)

---

## ۶. تحویل به مشتری

### گام A — ساخت توکن سفارشی (اتوماتیک!)
دو راه: توکن ساز آنلاین سایت (خودکار) یا اسکریپت ترمینال.

**راه ۱ — توکن ساز آنلاین:** به `https://rahmatpourmba-crypto.github.io/smart-contract-editor/` برو و دکمه 🧾 **توکن ساز** را بزن. فرم مشخصات (نام، نماد، عرضه، مالک، حالت پایه/پریمیوم + چک‌باکس‌های سفارشی) را پر کن — کد آماده می‌شود، خودکار کامپایل می‌شود و با ⬇️ دانلود `.sol` خروجی را بگیر. (هیچ تراکنش/کیف پولی لازم نیست.)

**راه ۲ — اسکریپت ترمینال** `make-token.js` را اجرا کن — خودش نسخه اختصاصی مشتری را می‌سازد، کامپایل می‌کند و در پوشه `orders/` ذخیره می‌کند:

```bash
cd smart-contract-editor
node make-token.js                                   # پرسیدن سوال‌ها
node make-token.js "Gold Token" GLD 1000000          # فرمت: نام نماد عرضه (پایه)
node make-token.js "Gold Token" GLD 1000000 0xAB0C...Owner  # + آدرس مالک (پایه)
# پریمیوم:
node make-token.js "Gold Token" GLD 1000000 premium 5 30 2
#   premium = حالت، 5 = مالیات٪، 30 = سوزاندن٪ از مالیات، 2 = سقف نهنگ٪ از عرضه
# با مالک مشخص:
node make-token.js "Gold Token" GLD 1000000 0xAB0C...Owner premium 5 30 2
```

نکته‌ها:
- بدون آدرس مالک → کیف پولی که قرارداد را deploy می‌کند مالک می‌شود.
- با آدرس مالک → همان آدرس مالک و دریافت‌کننده عرضه اولیه می‌شود.
- حالت `base` = MyToken (پایه)، حالت `premium` = PremiumToken (مالیات+ضد نهنگ).
- خروجی: `orders/GLD_20260806.sol` — همان فایل را برای مشتری بفرست.
- اگر آدرس مالک نامعتبر باشد، اسکریپت خطا می‌دهد و فایل ساخته نمی‌شود.
- پوشه `orders/` در gitignore است تا نسخه‌های هر مشتری لو نرود.

تست سریع هر فایل سفارشی (قبل از تحویل):
```bash
cd C:\Users\Admin\AppData\Local\Temp\tokentest
node test-custom.js C:\...\orders\GLD_20260806.sol
```
باید ببینی: `DEPLOY OK` + نام/نماد/عرضه/مالک درست + `ALL GOOD`.

### گام B — کامپایل و تست
قبل از تحویل، دوباره کامپایل کن (۰ خطا) و تست ganache را اجرا کن:
```bash
cd C:\Users\Admin\AppData\Local\Temp\tokentest
node test-token.js
```
نتیجه: `16 passed, 0 failed`

### گام C — تحویل فایل‌ها
به مشتری بده:
1. `MyToken.sol` (نسخه مخصوص او)
2. `ABI` و `Bytecode` کامپایل‌شده
3. راهنمای استقرار (بخش ۷)

تحویل به دو صورت:
- **اگر خودش استقرار می‌کند**: فقط کد + راهنما → بعد از پرداخت، آموزش بده.
- **اگر تو استقرار می‌کنی**: برای او تراکنش deploy را بفرست تا امضا کند (امن: تو کلیدش را نمی‌بینی).

### گام D — تایید
بعد از استقرار، آدرس توکن را به مشتری بده و ۳۰ دقیقه پشتیبانی برای تست انتقال/مint.

---

## ۷. راهنمای استقرار (برای مشتری)

### روش ۱ — با ادیتور آنلاین تو
به `https://rahmatpourmba-crypto.github.io/smart-contract-editor/` برو، کد را بگذار، کامپایل کن، ABI و Bytecode را بردار و با ابزار خودش deploy کند.

### روش ۲ — با Remix (رایگان)
1. برو به `https://remix.ethereum.org`
2. فایل `MyToken.sol` را بساز
3. تب **Compile** → نسخه 0.8.20+ → کامپایل
4. تب **Deploy** → محیط: **Injected Provider** → کیف پول متصل
5. پارامترها را وارد کن: نام، نماد، عرضه اولیه
6. **Deploy** و تراکنش را با MetaMask امضا کن

### روش ۳ — با Web3 Deploy (اختصاصی)
اگر می‌خواهی خودت deploy کنی، یک کیف پول جدید برای پروژه بساز، گس کافی بگذار و قرارداد را از ABI/Bytecode مستقر کن.

### هزینه‌های استقرار (گس) — برای گفتن به مشتری

> گس را کیف پولی می‌پردازد که تراکنش deploy را امضا می‌کند (معمولاً مشتری). تو به‌عنوان فروشنده هزینه مستقیم نداری — فقط زمان ساخت و تست.

| شبکه | هزینه تقریبی deploy | نکته |
|------|---------------------|------|
| BNB Smart Chain (BSC) | ۰.۰۱ تا ۰.۰۵ دلار | ارزان و سریع — پیشنهاد ما |
| Polygon | ۰.۰۱ تا ۰.۰۵ دلار | ارزان، پشتیبانی خوب |
| Ethereum | چند دلار (بستگی به شلوغی) | گران‌ترین |
| Tron | کم (هزینه انرژی) | نیاز به خرید TRX/Energy |
| Testnet (هیچ‌کدام) | رایگان | فقط برای تست — توکن رایگان شبکه تستی می‌گیری |

نکته‌ها برای فروش:
- برای مشتری کم‌هزینه، **BNB یا Polygon** را پیشنهاد بده.
- اگه تو deploy می‌کنی، تراکنش را برای مشتری بساز و بفرست تا خودش امضا کند — گس از کیف خودش می‌رود، امن است.
- Testnet رایگان است؛ می‌توانی اول روی آن دمو بدهی تا مشتری مطمئن شود، بعد روی شبکه اصلی.

---

## ۸. پول را چطور بگیرم؟

- **کریپتو**: USDT/USDC (BEP-20 یا TRC-20) — سریع‌ترین و بدون برگشت (chargeback)
- **پرفکت مانی / وب‌مانی**: برای مشتریان آسیای مرکزی
- **کارت به کارت / ریال**: برای مشتریان ایرانی
- **قانون طلایی**: ۵۰٪ قبل از تحویل کد، ۵۰٪ بعد از استقرار موفق.
- **پریمیوم**: چون مالیات روی تراکنش‌های خریدار اعمال می‌شود، حتماً در دمو/تست مشخص کن مالک معاف است و بقیه با چه درصدی مالیات می‌دهند.

> هشدار: هیچ‌وقت کد کامل را قبل از پرداخت کامل نده. فقط اسکرین‌شات یا دمو نشان بده.

---

## ۹. پیشنهادات فروش بعدی (upsell)

بعد از تحویل توکن، پیشنهاد بده:
- استقرار روی چند شبکه به‌صورت هم‌زمان
- پنل مدیریت توکن (وب‌اپلیکیشن برای mint/burn/lock)
- قرارداد استیکینگ یا باکس توزیع
- ساخت NFT اختصاصی
- لوگو و برندینگ توکن
- معرفی به DEX (پمپ لیکوئیدیتی روی PancakeSwap/Uniswap)

---

## ۱۰. نمونه آگهی فروش

```
✅ ساخت توکن ERC-20 اختصاصی
- کد استاندارد، بدون وابستگی خارجی
- قابل اجرا روی Ethereum / BSC / Polygon / Tron
- نسخه پایه: Mint, Burn, Lock, Ownership
- نسخه پریمیوم: مالیات + سوزاندن خودکار + ضد نهنگ + سقف تراکنش + وایت‌لیست + توقف اضطراری
- کامپایل و تست‌شده (تست سبز)
- تحویل فقط کد — بدون نیاز به کیف پول/گس برای تو
- پرداخت: USDT / کارت به کارت
💰 قیمت از 30 دلار | مشاوره رایگان
```

---

این سند را هر بار قبل از فروش بخوان تا چیز مهمی را فراموش نکنی.

---

## ۱۱. کیت فروش آماده

فایل [SALES-KIT.md](SALES-KIT.md) شامل: جواب‌های آماده برای مکالمه با مشتری (کپی و ارسال)، چک‌لیست قابل پرینت هر سفارش (گام‌به‌گام تا تحویل و پشتیبانی) و دستورهای سریع ساخت توکن. قبل از اولین فروش آن را پرینت بگیر.
